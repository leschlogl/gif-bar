name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: swift test (GIFBarKit)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: GIFBarKit/.build
          key: spm-${{ runner.os }}-${{ hashFiles('GIFBarKit/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-

      # Snapshot tests (…SnapshotTests) are skipped here: they were recorded on a Retina
      # display (2x backing scale) and this headless runner has no attached display, so
      # NSHostingView snapshots come back at a different pixel scale entirely — a gap no
      # amount of precision tolerance closes (verified: still failed at precision 0.98).
      # They remain a real local-dev safety net, just not CI-portable; run `swift test`
      # (no --skip) locally to exercise them.
      - name: swift test
        working-directory: GIFBarKit
        run: swift test --skip SnapshotTests

  app-build:
    name: xcodebuild (GIFBar)
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      # Real key isn't needed to compile — only Config/Secrets.xcconfig needs to exist,
      # since Config/Base.xcconfig #includes it. The app never calls the network during
      # `xcodebuild build`, only at runtime.
      - name: Set up placeholder secrets
        run: cp Config/Secrets.xcconfig.example Config/Secrets.xcconfig

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: GIFBarKit/.build
          key: spm-${{ runner.os }}-${{ hashFiles('GIFBarKit/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Build
        run: xcodebuild -project GIFBar.xcodeproj -scheme GIFBar build
