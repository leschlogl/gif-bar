/// Design values for the GIFBar popover, pulled from the approved Figma design
/// (`docs/decisions/gif-handling.md` and the imported Claude Design project).
/// Namespaced under `DesignTokens` rather than exposed as loose globals so call
/// sites read as `DesignTokens.Color.accent`, `DesignTokens.Radius.card`, etc.
public enum DesignTokens {}
